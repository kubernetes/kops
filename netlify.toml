# Netlify build for the kOps documentation site.
[build]
publish = "site"
command = "./hack/build-mdbook.sh build"

# Default pretty_urls (true) lets the legacy mkdocs-style /foo/ paths
# resolve to mdBook's /foo.html output without per-page rewrites.
