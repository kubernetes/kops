// To regenerate mesh.pb.go run make protobuf
syntax = 'proto3';

package mesh;

option go_package = "k8s.io/kops/protokube/pkg/gossip/mesh";

message KVState {
    map<string, KVStateRecord> records = 1;
}

message KVStateRecord {
    bytes data = 1;
	bool tombstone = 2;
	uint64 version = 3;
}
