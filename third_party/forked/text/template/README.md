# text/template

This is a copy of the Go standard library `text/template` package (from Go 1.26.5), together with a copy of the stdlib-internal `internal/fmtsort` package it depends on for sorted map iteration in `range`. Import paths are adjusted accordingly, parsing still uses the stdlib `text/template/parse` package, and the code is otherwise byte-identical to the upstream files.
