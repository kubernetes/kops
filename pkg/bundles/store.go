package bundles

/*
type Store interface {
}

type FSStore struct {
	base vfs.Path
}

var _ Store = &FSStore{}

func NewStore(p string) (Store, error) {
	base, err := vfs.Context.BuildVfsPath(p)
	if err != nil {
		return nil, err
	}

	return &FSStore{
		base: base,
	}, nil
}
*/
