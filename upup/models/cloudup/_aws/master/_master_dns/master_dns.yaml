# Configuration for a DNS name for the master

dnsZone/{{ .DNSZone }}: {}

{{ if DNSZonePrivate }}

dnsZoneAssociation/{{ .DNSZone }}-{{ ClusterName }}:
  vpc: vpc/{{ ClusterName }}
  dnsZone: dnsZone/{{ .DNSZone }}

{{ end }}