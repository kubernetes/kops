# See https://cloud.google.com/cloud-build/docs/build-config
timeout: 1800s
options:
  substitution_option: ALLOW_LOOSE
  machineType: "E2_HIGHCPU_32"
steps:
  # Build & Push the images
  - name: "mirror.gcr.io/library/golang:1.26.3-trixie"
    id: images
    entrypoint: make
    env:
      - CI=$_CI
      - DOCKER_IMAGE_REGISTRY=$_DOCKER_IMAGE_REGISTRY
    args:
      - kops-utils-cp-push
      - kops-controller-push
      - kops-channels-push
      - dns-controller-push
      - kube-apiserver-healthcheck-push
      - discovery-server-push
  # Build & push the binaries
  - name: "mirror.gcr.io/library/golang:1.26.3-trixie"
    id: binaries
    entrypoint: make
    env:
      # _GIT_TAG is not a valid semver, we use CI=1 instead
      # - VERSION=$_GIT_TAG
      - CI=$_CI
      - GCS_BUCKET=$_GCS_BUCKET
      - FASTLY_SERVICE_NAME=$_FASTLY_SERVICE_NAME
    secretEnv:
      - FASTLY_API_TOKEN
    args:
      - gcs-upload-and-tag
substitutions:
  # _GIT_TAG will be filled with a git-based tag for the image, of the form vYYYYMMDD-hash, and
  # can be used as a substitution
  _CI: "1"
  _GIT_TAG: "12345"
  _PULL_BASE_REF: "dev"
  _DOCKER_IMAGE_REGISTRY: gcr.io/k8s-staging-kops
  _GCS_BUCKET: gs://k8s-staging-kops
  _FASTLY_SERVICE_NAME: dl.k8s.io

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/fastly-purge-token/versions/latest
      env: FASTLY_API_TOKEN
